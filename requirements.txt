# ── Core (existing) ───────────────────────────────────────────────────────────
streamlit
opencv-python
numpy
scipy
pytesseract          # optional — timestamp OCR; graceful fallback if absent

# ── CNN embedding (Strategy 1) ────────────────────────────────────────────────
# PyTorch and torchvision are optional. The CNN tab simply does not appear in
# the UI when these packages are absent. For platform-specific install commands
# (CUDA, Metal, CPU-only) see: https://pytorch.org/get-started/locally/
torch
torchvision
pillow               # required by torchvision transforms
